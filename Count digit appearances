class Solution:
    def countDigitOccurrences(self, nums: list[int], digit: int) -> int:
        c=0
        for i in nums:
            while i>0:
                k=i%10
                if k==digit:
                    c+=1
                    i//=10
                else:
                    i//=10
        return c
